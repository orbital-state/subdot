import React from 'react';
import { render, Text } from 'ink';

export function startTUI() {
  const App = () => (
    <Text>
      🚀 Subdot TUI coming soon... (Monitoring events)
    </Text>
  );

  render(<App />);
}
